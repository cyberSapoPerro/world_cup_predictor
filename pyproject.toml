[project]
name = "world_cup_predictor"
version = "0.1.0"
description = "A simple World Cup match score predictor based on an Elo model"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pandas",
    "rich>=15.0.0",
    "scipy",
]
